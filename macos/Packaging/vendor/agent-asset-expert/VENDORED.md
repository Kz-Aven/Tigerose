Agent Asset Expert 0.1.0 is bundled as a local dependency because Tigerose's
execution recorder imports it at runtime. Distributed under the included MIT
LICENSE. Source snapshot: commit `54c0c3ccbdb11dd5d15d38f560608574114c9670`
of the local `agent-asset-expert` project, captured on 2026-09-24.

Only `pyproject.toml`, `LICENSE`, and `src/` are included; no user data or editable
installation is required. This package's CLI is an internal dependency entry
point, not a separately supported Tigerose product.
